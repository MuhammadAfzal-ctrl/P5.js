function setup() {
  createCanvas(600, 400); 
  background(250,250,100)
}

function draw() {
  
  redcap = random(0,255);
  bluecap= random(0,255);
  greencap= random(0,255);
  spotx= random(0,600);
  spoty= random(0,400);
  fill(redcap,bluecap,greencap)
  ellipse(spotx,spoty,10,10)
 
}
